CREATE DATABASE IF NOT EXISTS hatim_erp CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE hatim_erp;

-- قاعدة البيانات جاهزة
-- سيتم إنشاء الجداول تلقائياً عند تشغيل المشروع (synchronize: true)
